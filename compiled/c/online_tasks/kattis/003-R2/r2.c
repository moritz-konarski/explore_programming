#include <stdio.h>

/*find the second initial number given one of them and the average*/

int main() {

    int s, r1;

    scanf("%d %d", &r1, &s);

    printf("%d", 2 * s - r1);

    return 0;
}